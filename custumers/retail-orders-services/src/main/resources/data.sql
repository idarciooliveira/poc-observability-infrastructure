-- No seed rows needed: the product catalog is a static map in
-- CreateOrderUseCase (PROD-001..003, synthetic IDs, no PII).
-- Kept as a no-op so spring.sql.init.mode=always has a valid script.
SELECT 1;
